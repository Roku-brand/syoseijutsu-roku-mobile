# 処世術禄 フロントエンド正規仕様

## 位置づけ

この文書は、2026-08-30に公開サイト `https://shoseijutsuroku.com/` をChromeで実査した結果を基準にした現行フロントエンド仕様である。画面の見た目・文言・導線は、README、過去レポート、未使用コードよりも実際にブラウザで表示されるUIを優先する。

確認ビューポートはPC 1440×900、スマホ 390×844。既存の公開コンテンツ、ID、本文、人物像分類は仕様確認の対象であり、今回変更しない。

## 正規画面とルート

| 画面 | 正規ルート | 役割 |
| --- | --- | --- |
| Welcome | `/welcome`、初回の `/` | 無料開始と完全版購入への入口、収録数と利用方法の説明 |
| ホーム | `/`、`/(tabs)` | 今日の処世術、学習導線、完全版案内 |
| 探す | `/discover` | 処世術・理論の検索、カテゴリ・人物像への入口 |
| 学ぶ | `/learn`、`/(tabs)/learn` | 3ステージ、ケース学習、進捗表示 |
| マイページ | `/my-os`、`/(tabs)/my-os` | 利用状態、履歴、設定・購入復元への入口 |
| カテゴリ | `/category/[key]` | 対人術・仕事術・人生術のテーマ一覧 |
| テーマ | `/theme/[category]/[title]` | 人物像一覧 |
| 人物像 | `/subcategory/[category]/[name]` | 人物像に属する処世術一覧 |
| 処世術詳細 | `/card/[id]` | 処世術本文、実践、注意点、関連理論 |
| 理論詳細 | `/theory/[id]` | 理論の概要・要約、関連処世術・関連理論 |
| 学習ケース詳細 | `/learn/[caseId]` | ケース本文と学習導線 |
| 購買 | `/upgrade` | 完全版の説明、価格、購入・復元 |
| プライバシー | `/legal/privacy` | プライバシーポリシーの直リンク表示 |

`/(tabs)/catalog` は旧URLから `/discover` へ転送する互換ルートとして保持する。設定、履歴、ライブラリ、認証、管理者向け画面は現行実装の補助ルートであり、公開主ナビゲーションには表示しない。

## 正規ナビゲーション

デスクトップでは左側の固定ナビゲーション（実測幅約92px）、スマホでは画面下部の固定ナビゲーションを使用する。主項目は「ホーム」「探す」「学ぶ」「マイページ」。共通ヘッダーの操作は「原則」「設定」で、詳細画面では戻る導線を表示する。

Welcomeと購買ページは通常のタブナビゲーションを表示しない独立導線である。Welcomeの「無料ではじめる」はアプリ本体へ、「完全版を購入する」は `/upgrade` へ遷移する。

## 無料版・完全版

- 無料版はログインなしで開始でき、45件の処世術、45件の理論、7件の学習ケースを利用できる。
- 完全版は30日間・一回払い・自動更新なしで、356件の処世術、630件の理論、21件の学習ケースを利用できる。
- 無料版で制限された内容はロック表示を維持し、購入ページへ誘導する。
- 購入・復元・アクセス状態の判定は既存の `src/access`、`src/auth`、`src/state` の仕様を維持する。
- 本番Chrome確認で既存セッションがオーナー状態を保持していたため、所有者表示はそのセッションの実測値として扱い、匿名利用者の状態とは混同しない。

## カードと表示ルール

検索結果、カテゴリ、テーマ、人物像、処世術、理論は、現行のカード構造・タイトル・折り返しを維持する。カテゴリカードはデスクトップで横方向に並び、実測約328×172px。390px幅では1列となり、左右12px余白・幅約366px・高さ約172pxで表示する。人物像一覧はデスクトップで複数列、スマホで1列を基本とする。

長文の詳細・法務ページは縦スクロールする。0件、ロック、空状態は既存の `EmptyState` とロック表示を使い、カードやナビゲーションを崩さない。理論・処世術タイトルは、PC・スマホともに内容に応じて自然に折り返す。

## PC・スマホ方針

レスポンシブ判定は、初期サーバー描画とクライアント描画で異なる幅を直接参照して表示が変わらないよう、hydrated後のウィンドウ寸法フックを使う。PCは左ナビと複数列、スマホは下ナビと1列・コンパクトヘッダーを維持する。ブレークポイントや既存の余白・文字サイズは、UI確認なしに変更しない。

## 画面の参照データ

- カタログ、カテゴリ、人物像、処世術、理論: `src/data/catalog.ts` および生成元・runtimeデータ
- 学習ケースとステージ: `src/data/learning.ts`、`src/data/learning.runtime.ts`
- 無料公開範囲: `src/access/access-config.ts`
- 利用状態: `src/access/access-state.tsx`、`src/state/app-state.tsx`
- 共通ヘッダー・詳細導線: `src/components/book-ui.tsx`
- 共通下部ナビ: `src/components/persistent-bottom-nav.tsx`
- 共通テキスト・ボタン・セクション見出し: `src/components/ui.tsx`

表示件数は可能な箇所で参照データから導出し、手書きの件数を増やさない。本文・ID・タイトルの内容自体は正本データを変更しない。

## 今後変更してはいけない仕様

現行UIの導線、タブ項目、無料版のロック、完全版の購入導線、カード構造、タイトルの折り返し、デスクトップ左ナビ、スマホ下ナビ、直接アクセス可能な法務ページを、過去コードや文書だけを根拠に戻したり変更したりしない。変更が必要な場合は、まず公開サイトのPC・スマホ実測と主要ルートの回帰確認を行う。
