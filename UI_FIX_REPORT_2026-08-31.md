# UI修正レポート（2026-08-31）

## 対象

PC幅とスマホ幅のWeb UIを目視確認し、検索欄、人物像一覧、人物像の処世術一覧、理論一覧を中心に表記とレイアウトを点検しました。添付画像は検索欄の表示参考として扱いました。

## 修正内容

- 検索アイコンを共通コンポーネント化し、探す画面・理論一覧・理論カテゴリ・人気検索チップで同じ虫眼鏡の図形を使用。
- 検索入力の内側余白、アイコンの固定幅、入力文字の最小幅を明示し、狭い画面で虫眼鏡とプレースホルダーが押し合わないよう修正。
- 人物像／理論の一覧リンクを狭い画面で折り返せるようにし、見出し・リンクの重なりを防止。
- 人物像の処世術一覧で、モバイルの `flex: 0` が一覧の高さを0として扱う問題を修正。`14 / 14` の完了カウンターが先頭行へ重なる状態を解消し、一覧末尾に表示。
- 完了カウンターの左右に余白と背景を設定し、区切り線が `14 / 14` の文字を横切らないよう修正。

## 確認結果

- `pnpm typecheck`：成功
- `pnpm export:pages`：成功（Web静的出力・GitHub Pages用フォールバック・SEO資産を生成）
- `pnpm test:e2e -- --reporter=dot`：48 passed
- 目視確認：390×844（スマホ）、1280×720（PC）で検索画面・理論一覧・人物像一覧を確認

## 公開

GitHub Pagesのデプロイワークフローが `main` ブランチへの反映後に実行される構成です。本レポートと修正コードを同じリリースとして公開します。
