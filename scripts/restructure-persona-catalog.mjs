import { readFile, writeFile } from 'node:fs/promises';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const techniquesPath = path.join(root, 'src', 'data', 'generated', 'techniques.json');
const metadataPath = path.join(root, 'src', 'data', 'generated', 'metadata.json');

const leadershipCards = [
  ['目的を一文で共有する', '人は作業の指示より、その仕事が何を変えるのかを理解したときに、自分で判断して動きやすくなる。', 'チームの仕事が細かな指示だけで始まると、想定外の場面で各自が止まりやすい。目的が共有されていれば、手段を変える必要が出ても「何を守るべきか」を基準に判断できる。会議の冒頭や依頼の際に、作業内容の前に「誰にどんな価値を届けるのか」を一文で置く。目的は長い理念ではなく、現場が優先順位を迷ったときに戻れる言葉にする。', 3],
  ['成功の基準を先にそろえる', '評価の物差しが曖昧な集団では、努力していても互いに違う成果を追い、最後にだけ認識のずれが表面化する。', '「良い仕事」の意味を各自が補って進めると、納期を重視する人と品質を重視する人が、同じ案件で反対方向へ走ることがある。着手前に完成の条件、優先順位、見直す時点を言葉にすると、途中の判断がそろいやすい。数字だけで定義しきれない仕事でも、誰が見て何をもって完了とするかを確認する。基準を先に置くことは監視ではなく、任せるための土台になる。', 3],
  ['決める人と任せる人を分ける', '責任の所在が曖昧なまま全員で決めようとすると、参加は増えても決定が遅れ、誰も最後の判断を引き受けなくなる。', '意見を集めることと、決定を分散させることは別である。会議で多くの人に聞いたのに結論が出ないなら、誰が最終判断をし、誰が実行上の裁量を持つかが曖昧になっている。案件ごとに決定者、相談相手、実行担当を短く明示する。任せた領域には途中で細かく介入せず、判断の境目だけを合意しておくと、速さと当事者意識を両立しやすい。', 3],
  ['現場の声が戻る経路をつくる', '上に届く情報が成功報告だけになると、組織は問題が大きくなってからしか方向を修正できなくなる。', '立場が上がるほど、遠慮や選別によって実態が見えにくくなる。定例の一対一、匿名の意見箱、短い振り返りなど、悪い知らせほど早く上がる経路を複数持つことが必要になる。大切なのは「何かある？」と聞くだけでなく、報告を受けたときに誰の責任かを急いで決めないことだ。小さな違和感を歓迎する反応が続くと、現場は早めに相談しやすくなる。', 2],
  ['反対意見を早く出せる空気をつくる', '反対が消えた集団は合意したのではなく、異論を出すコストが高くなっていることがある。', '会議で全員がすぐ賛成すると、決定は速く見えるが、実行段階で不満やリスクが噴き出しやすい。とくに地位差がある場では、先に賛成を示すほど別の意見は言いにくくなる。リーダーが先に懸念を問い、反対した人を評価し、意見と人格を切り分けて扱う。結論を急ぐ前に異論を出せる数分を設けるだけでも、判断の質は変わる。', 3],
  ['判断の理由を隠さない', '納得できない決定でも、判断の筋道が見えると、人は自分が恣意的に扱われたとは感じにくい。', 'すべての希望を通せない場面で、結果だけを伝えると、選ばれなかった側は自分や仕事が軽く見られたと受け取りやすい。何を優先し、どの制約があり、次に見直す条件は何かを簡潔に伝える。細部を全部公開する必要はないが、判断の原理を共有すると、次の提案も同じ基準で考えられる。説明は反論を封じるためでなく、チームの判断力を育てるために行う。', 2],
  ['役割を強みで配る', '役割が肩書きだけで決まると、得意な力が眠り、苦手な仕事を抱えた人ほど消耗しやすくなる。', '経験年数や空いている人だけで仕事を振ると、短期的には埋まっても、チーム全体の成果は伸びにくい。誰が構想をつくり、誰が詰め、誰が関係者を動かせるかを見て、役割を組み合わせる。本人にも得意な場面と伸ばしたい領域を聞き、固定化しすぎないことが重要だ。強みで任せることは特別扱いではなく、互いの力を補完する設計である。', 2],
  ['期待を行動で具体化する', '抽象的な期待は受け手ごとに別の行動へ翻訳されるため、評価の場で初めてずれが見えやすい。', '「主体性を持って」「もっと協力して」と伝えても、何を変えればよいかは人によって異なる。期待する行動を、会議前に論点を出す、困りごとを翌日までに共有するなど、観察できる形に置き換える。基準が見えると、本人は試しやすく、上司側も印象だけで評価しにくくなる。一度に多くを求めず、次の一週間で見たい行動を一つか二つに絞る。', 2],
  ['小さな前進を見えるようにする', '成果が遠い仕事では、進んでいる実感が失われるほど、努力は個人の中で孤立しやすくなる。', '長期案件は完成まで評価が見えず、チームの熱量が落ちやすい。中間の節目、解決した課題、顧客からの反応を短く共有すると、自分たちの仕事が前に進んでいると確かめられる。大げさな称賛より、何がどこまで変わったかを具体的に示す方が次の行動につながる。進捗表や週次の共有では、未完了だけでなく前進も一行残す。', 2],
  ['成果の手柄をチームに返す', '成果を独占するリーダーの下では、協力は次第に投資ではなく消耗として受け取られやすくなる。', '目立つ人だけが評価されると、裏方や専門担当は次に力を貸す理由を失いやすい。成果を発表するときは、誰の判断、準備、支援が結果を支えたかを具体的に示す。個人の貢献を見える形で返すと、周囲は努力が組織の中で消えないと学ぶ。ただし全員を同じ言葉で褒めるのではなく、実際の役割に沿って功績を伝える。', 2],
  ['失敗を責める前に仕組みを直す', '同じ失敗が繰り返されるとき、原因を個人の注意力だけに置くほど、組織は再発条件を温存してしまう。', 'ミスが起きた直後は誰が悪いかを決めたくなるが、急いで個人を責めると、次から問題が隠されやすくなる。手順、情報の渡し方、確認の時点、負荷の偏りを振り返り、次に同じ状況でも防げる仕組みを一つ変える。責任を曖昧にするのではなく、責任を取ることと学ぶことを分ける。振り返りでは「次回どこを変えるか」を必ず決めて終える。', 3],
  ['困難な判断を先送りしない', '曖昧な状態を長く残すほど、現場は自分で補えない不確実性のコストを払い続ける。', '優先順位の変更、人員配置、問題行動への対応などは、判断を遅らせるほど周囲に迷いと憶測を生む。情報が十分でない場合でも、いつまでに何を基準に決めるかを明示し、暫定の方針を出す。結論を急ぐことと、放置しないことは違う。決められない理由を共有し、次の判断時点を置くことで、チームは待つ間にも動きやすくなる。', 3],
  ['優先順位を繰り返し示す', '一度伝えた方針でも、日々の依頼や評価が別の方向を向くと、現場は実際に報われる行動を優先する。', '忙しい組織では、新しい依頼が来るたびに本来の重点が薄れていく。リーダーが毎週同じ優先順位を確認し、追加する仕事には「何を後ろへ回すか」まで添えると、現場は断る判断をしやすい。言葉だけでなく、会議時間、予算、評価の配分も優先順位とそろえる。繰り返すことはしつこさではなく、分散する注意を一つの方向へ戻す仕事である。', 3],
  ['会議で結論と次の行動を残す', '話し合いの満足感は、決定・担当・期限が残らなければ、実行の責任を誰も持たない状態に変わりやすい。', '会議では議論が盛り上がっても、終わった後に各自の理解が少しずつ違うことがある。終了前に、何を決めたか、誰が何をいつまでにするか、未決の論点は何かを短く確認する。記録は議事録を完璧に作るためでなく、行動の受け渡しを明確にするために残す。リーダー自身がこの型を徹底すると、会議は情報交換から前進を生む場へ変わる。', 3],
  ['一対一で状態を確かめる', '集団の場では人は役割を演じやすく、個人の迷いや負荷は表面に出にくい。', '定例会議では問題がなさそうでも、担当者が優先順位に迷っていたり、関係者との摩擦を抱えていたりすることがある。一対一では成果確認だけでなく、何に時間を取られ、どこで判断に詰まっているかを聞く。助けるためにすぐ答えを出すより、本人の見立てを確かめて必要な支援を決める。短くても継続した対話があると、問題は大きくなる前に扱いやすい。', 2],
  ['守る境界を明確にする', '何でも引き受ける組織は協力的に見えても、優先順位と責任範囲が崩れるほど、重要な仕事を守れなくなる。', '他部署からの依頼や顧客の要望に毎回応じると、チームは評価されない雑務に追われることがある。どこまで対応し、何は別の担当へ渡し、緊急時だけ何を例外にするかを前もって決める。断る際は相手を拒むのでなく、守るべき目的と代替案を示す。リーダーが境界を引くことで、メンバーは本来の責任を果たしやすくなる。', 2],
  ['異なる意見を統合する', '対立した意見の多くは結論の違いではなく、互いが守ろうとしている価値の違いから生まれる。', '速度を重視する人と品質を重視する人がぶつかるとき、どちらかを黙らせても不満は残る。まず各案が避けたい失敗と守りたい価値を言葉にし、両方を満たす条件がないかを探す。二者択一にする前に、段階導入や検証期間など第三の案をつくる。リーダーの役割は多数決を急ぐことより、対立を判断材料へ変えることである。', 3],
  ['自ら約束を守る', 'リーダーの日常の振る舞いは、明文化されたルールより早く、組織で許される行動の基準になる。', '期限、返信、会議の準備といった小さな約束を上の立場の人が軽く扱うと、チームは言葉ではなく行動を手本にする。守れない事情が出たときは、遅れる前に伝え、代わりの期限を示す。例外をつくるなら理由も明らかにする。厳しく管理する前に自分の振る舞いを整える方が、基準は自然に共有されやすい。', 3],
  ['育成を仕事として時間を取る', '人を育てる時間を余剰として扱う組織では、忙しさが続くほど次の担い手が育たず、さらに忙しさが固定化する。', '目の前の成果を急ぐと、できる人に仕事を集める方が早く見える。しかしその状態が続けば、任せられる範囲は広がらず、重要な判断も一人に集中する。業務の中で少し難しい役割を渡し、途中で考え方を振り返る時間を予定に入れる。育成は講義だけでなく、任せて振り返り、次の挑戦につなげる循環である。', 2],
  ['退く判断もリーダーが引き受ける', '続ける理由より撤退基準を先に持つことで、過去の投入に引かれず、限られた資源を次の可能性へ戻せる。', '始めた施策を止めることは失敗に見えやすく、誰も中止を言い出せなくなる。だが成果が出ない理由を検証し、あらかじめ置いた基準に照らして終える判断は、チームの時間と信頼を守る。中止するときは努力を否定せず、何を学び、何を残し、どこへ資源を移すかを説明する。撤退を引き受ける姿勢があると、メンバーも試行を恐れにくくなる。', 2],
];

const catalog = JSON.parse(await readFile(techniquesPath, 'utf8'));
function getPersona(field, name) {
  const category = catalog.categories.find((entry) => entry.name === field);
  if (!category) throw new Error(`Category not found: ${field}`);
  const index = category.subcategories.findIndex((entry) => entry.name === name);
  if (index < 0) throw new Error(`Persona not found: ${field} / ${name}`);
  return { category, index, persona: category.subcategories[index] };
}
function splitPersona(field, name) {
  const { category, index, persona } = getPersona(field, name);
  if (persona.items.length !== 26) throw new Error(`${name} must have 26 cards before the split.`);
  const makePart = (suffix, items) => ({ ...persona, name: `${name}${suffix}`, articleTitle: `${name}${suffix}`, items: items.map((item, order) => ({ ...item, persona: `${name}${suffix}`, displayOrder: order + 1 })) });
  category.subcategories.splice(index, 1, makePart('①', persona.items.slice(0, 13)), makePart('②', persona.items.slice(13)));
}
splitPersona('対人術', '人たらしの人');
splitPersona('仕事術', '交渉がうまい人');
{
  const { persona } = getPersona('仕事術', 'タスク処理がうまい人');
  const removed = new Set(['中断した場所を残しておく', '疲れる前提で予定を組む']);
  persona.items = persona.items.filter((item) => !removed.has(item.title)).map((item, order) => ({ ...item, displayOrder: order + 1 }));
  if (persona.items.length !== 20) throw new Error('Task-processing persona must have 20 cards after removal.');
}
{
  const { persona } = getPersona('対人術', '人を動かせる人');
  persona.articleTitle = '人を動かせる人';
  persona.items = leadershipCards.map(([title, essence, explanation, importance], index) => ({ id: `leadership-${String(index + 1).padStart(2, '0')}`, field: '対人術', persona: '人を動かせる人', title, essence, explanation, importance, relatedTheoryIds: [], subtitle: essence, displayOrder: index + 1, status: 'published' }));
}
const cards = catalog.categories.flatMap((category) => category.subcategories.flatMap((persona) => persona.items));
const overLimit = catalog.categories.flatMap((category) => category.subcategories).filter((persona) => persona.items.length > 20).map((persona) => `${persona.name}: ${persona.items.length}`);
if (overLimit.length) throw new Error(`Persona card limit exceeded: ${overLimit.join(', ')}`);
if (new Set(cards.map((card) => card.id)).size !== cards.length) throw new Error('Technique ids must remain unique.');
await writeFile(techniquesPath, `${JSON.stringify(catalog, null, 2)}\n`, 'utf8');
const metadata = JSON.parse(await readFile(metadataPath, 'utf8'));
metadata.techniqueCount = cards.length;
metadata.personaCount = catalog.categories.reduce((total, category) => total + category.subcategories.length, 0);
await writeFile(metadataPath, `${JSON.stringify(metadata, null, 2)}\n`, 'utf8');
console.log(JSON.stringify({ techniques: cards.length, personas: metadata.personaCount, maxPerPersona: 20 }, null, 2));
